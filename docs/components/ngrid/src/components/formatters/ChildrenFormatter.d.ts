import * as React from "react";
type ChildrenFormatterProps = {
    children?: React.ReactNode;
    maxLength?: number;
};
declare const ChildrenFormatter: React.FC<ChildrenFormatterProps>;
export default ChildrenFormatter;
