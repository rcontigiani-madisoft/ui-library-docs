import { Entity } from '../../typings';
import { ActionProps } from './row-action.types';
export declare function LinkAction<T extends Entity>({ action, item, gridId, ariaLabel, children, }: ActionProps<T>): import("react/jsx-runtime").JSX.Element;
