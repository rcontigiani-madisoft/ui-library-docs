import { Entity } from '../../typings';
import { ActionProps } from './row-action.types';
export declare function AjaxAction<T extends Entity>({ action, item, children, errorTitle, ariaLabel, }: ActionProps<T>): import("react/jsx-runtime").JSX.Element;
