type Props = {
    currentPage: number;
    itemsCountPerPage: number;
    totalItemsCount: number;
};
declare function PaginationText({ currentPage, itemsCountPerPage, totalItemsCount, }: Props): import("react/jsx-runtime").JSX.Element;
export default PaginationText;
